# This file is part of the Ocrend Framewok 3 package.
#
# (c) Ocrend Software <info@ocrend.com>
#
# For the full copyright and license information, please view the LICENS
# file that was distributed with this source code.

# Configuración de la base de datos
database:
  default_driver: mysql
  drivers:
    mysql: 
      host: localhost
      user: root
      pass:
      name: ocrend
      port: default
      socket: default
    sqlite3:
      file: '___ROOT___Ocrend/Kernel/Database/Drivers/Sqlite/db.sqlite3'
      
# Configuración de servidor SMTP
mailer:
  host: 
  user: 
  pass: 
  port: 587
  logo:

# Configuración de twig
twig:
  cache: false
  compiled_dir: ./app/templates/.compiled/
  charset: UTF-8
  strict_variables: false
  autoescape: html
  
# Configuración del sitio
build:
  production: false
  name: Ocrend Framework 3
  url: http://127.0.0.10/proyectos/ocrend3/
  timezone: America/Caracas
  version: 3.0.1

# Configuración de cookies
sessions:
  lifetime: 3600
  unique: KRWYSHD2012
  user_cookie:
    enable: true
    key_encrypt: Kr10770JJH
    lifetime: 3600

# Configuración de la api
api:
  active: true
  origin: '*'

# Configuración de ruta
router:
  ssl: false
  path: 127.0.0.1/proyectos/ocrend3/